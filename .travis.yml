language: python
python:
 - 2.7
 - 3.5

# - pip install -r requirements.txt
before_install:
 - python setup.py install
 - python bootstrap.py
 - ./bin/buildout

script:
 - nosetests -i regulartest -i sphinxtest
